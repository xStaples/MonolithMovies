export class LoginTemplete {
    loginUsername: string;
    loginPassword: string;

    constructor(loginUsername: string, loginPassword: string){
        this.loginUsername = loginUsername;
        this.loginPassword = loginPassword;
    }
}